import { createMuiTheme } from "@material-ui/core";

export const arcserveMuiTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#ffeb3b",
    },
    secondary: {
      main: "#9ccc64",
    },
  },
});
